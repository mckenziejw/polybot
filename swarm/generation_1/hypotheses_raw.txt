

```json
[
  "Opening auction imbalance decay rate: Measure how fast the bid-ask imbalance at market open (first 2s) mean-reverts over the next 10-20s. Markets where imbalance persists (slow decay) indicate informed flow with directional conviction, while fast mean-reversion indicates noise/MM repositioning. Trade in the direction of slow-decaying imbalances, fade fast-reverting ones.",

  "Cross-timeframe liquidity regime transfer: Use the 4h market's final orderbook state (last snapshot before resolution) as a prior for the overlapping 5m market's opening regime. If the 4h book ended with tight spreads and symmetric depth, the 5m market opens in a 'consensus' regime (fade extremes); if it ended skewed or wide-spread, the 5m opens in a 'discovery' regime (follow early directional signals). This exploits the ~139 overlap-period markets where both timeframes exist.",

  "Spread-volatility divergence: Compute the ratio of orderbook spread width to recent BTC realized volatility (e.g., 30s RV from trade timestamps). When spreads are unusually wide relative to current vol, MMs are pricing in private information about upcoming moves — go with the direction of the heavier side. When spreads are tight relative to vol, MMs are complacent — fade the current book imbalance as it reflects stale positioning."
]
```
